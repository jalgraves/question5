apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "question5.fullname" . }}
  labels: {{ include "question5.labels" . | nindent 4 }}

spec:
  progressDeadlineSeconds: 300
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: 10
  selector:
    matchLabels: {{ include "question5.selectorLabels" . | nindent 6 }}
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels: {{ include "question5.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: VERSION
          value: {{ .Chart.AppVersion }}
      {{- if eq .Values.env "dev" }}
        image: "{{ .Values.image.name}}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: Never
      {{- else }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: Always
      {{- end }}
        name: question5
        ports:
        - containerPort: {{ .Values.containerPort }}
          name: {{ include "question5.targetPort" . }}
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          privileged: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        resources: {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          exec:
            command:
            - cat
            - /app/package.json
          initialDelaySeconds: 5
          periodSeconds: 60
        readinessProbe:
          httpGet:
            path: /healthz
            port: {{ include "question5.targetPort" . }}
          initialDelaySeconds: 5
          failureThreshold: 6
          periodSeconds: 10
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ include "question5.serviceAccountName" . }}
      {{- end }}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
